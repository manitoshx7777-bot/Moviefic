:root{
  --mcu-red:#FF0000;
  --mcu-yellow:#FFFF00;
  --mcu-blue:#0080ff;
  --bg:#000;
  --glass: rgba(255,255,255,0.04);
  --card-radius:12px;
  --max-width:1200px;
}
*{box-sizing:border-box}
html,body{height:100%;margin:0;background:var(--bg);color:#fff;font-family: "Segoe UI", Roboto, system-ui, sans-serif;}

/* STARFIELD CANVAS */
#starfield{position:fixed;inset:0;z-index:0;display:block}

/* INTRO (centered cinematic) */
#intro{
  position:fixed;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;
  z-index:6;text-align:center;padding:20px;pointer-events:none;
}
#intro .title{
  font-weight:900;line-height:0.95;letter-spacing:6px;
  color:var(--mcu-red);text-shadow:0 0 8px var(--mcu-yellow),0 0 28px rgba(255,255,0,0.12);
  font-size:clamp(40px,8vw,120px);transform:none;opacity:0;transition:opacity .9s ease,transform 1s ease;
  -webkit-font-smoothing:antialiased;
}
#intro.show .title{opacity:1}
#intro.moveUp .title{transform:translateY(-220px) scale(.92);}

/* header/banner after intro */
header.mcu-header{position:relative;z-index:5;width:100%;display:none;align-items:center;justify-content:center;padding:38px 20px;background:linear-gradient(180deg, rgba(0,0,0,0.35), rgba(0,0,0,0.2));backdrop-filter:blur(2px);}
header.mcu-header.active{display:flex;flex-direction:column;gap:8px}
header.mcu-header h1{margin:0;color:var(--mcu-red);text-shadow:2px 2px 6px var(--mcu-yellow);font-size:clamp(20px,4vw,38px);letter-spacing:4px;font-weight:800}
header.mcu-header p{margin:0;color:#fff;opacity:.95;font-size:clamp(14px,2.2vw,18px)}

/* MENU / hamburger + spotlight */
.menu-btn{
  position:fixed;top:14px;left:14px;z-index:8;background:transparent;border:0;color:#fff;font-size:28px;padding:8px;cursor:pointer;
  display:flex;align-items:center;justify-content:center;border-radius:8px;backdrop-filter: blur(4px);
}
.menu-btn:focus{outline:2px solid var(--mcu-blue)}
.menu-body{position:fixed;top:54px;left:14px;background:rgba(17,17,17,0.95);padding:12px 14px;border-radius:10px;display:none;z-index:8}
.menu-body.show{display:block}
.menu-body a{display:block;color:#fff;text-decoration:none;padding:8px 6px;border-radius:6px}
.menu-body a:hover{color:var(--mcu-blue);background:rgba(255,255,255,0.03)}
.spotlight{margin-top:10px;padding:10px;border-radius:8px;background:linear-gradient(90deg, rgba(255,255,255,0.02), rgba(255,255,0,0.02));font-size:13px}
.spotlight img{width:44px;height:44px;border-radius:6px;object-fit:cover;margin-right:8px;vertical-align:middle}

/* CONTROLS */
.controls{width:100%;display:flex;justify-content:center;gap:12px;padding:18px 12px;background:transparent;z-index:4}
.controls .group{display:flex;gap:8px;align-items:center}
.pill{background:var(--glass);border:1px solid rgba(255,255,255,0.04);padding:8px 12px;border-radius:999px;color:#fff;cursor:pointer;user-select:none}
.pill.active{box-shadow:0 0 18px var(--mcu-yellow);border-color:var(--mcu-yellow)}
.search{padding:8px 12px;border-radius:10px;background:#111;border:1px solid rgba(255,255,255,0.03);min-width:240px;color:#fff}
.toggle{display:flex;align-items:center;gap:6px}

/* LAYOUT */
.container{max-width:var(--max-width);margin:22px auto;padding:0 16px;position:relative;z-index:3}
.timeline-wrap{overflow-x:auto;white-space:nowrap;padding:14px 10px;scroll-snap-type:x mandatory;-webkit-overflow-scrolling:touch}
.chip{display:inline-block;width:120px;margin:6px;border-radius:10px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0.25));padding:8px;cursor:pointer;scroll-snap-align:center;transition:transform .18s ease, box-shadow .18s}
.chip:hover{transform:translateY(-6px);box-shadow:0 12px 30px rgba(0,0,0,0.6), 0 0 22px rgba(255,255,0,0.06)}
.chip img{width:100%;height:66px;object-fit:cover;border-radius:6px;display:block}
.chip .meta{font-size:12px;margin-top:6px}
.chip .meta .title{font-weight:700;color:#fff}
.chip .meta .year{font-size:11px;opacity:.8;color:#ddd}

/* GRID (below timeline) */
.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:18px;padding:22px 0}
.card{background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0.25));border-radius:var(--card-radius);overflow:hidden;cursor:pointer;transition:transform .18s ease, box-shadow .18s}
.card:hover{transform:translateY(-8px);box-shadow:0 18px 40px rgba(0,0,0,0.6),0 0 32px rgba(255,255,0,0.04)}
.card .poster{height:280px;width:100%;object-fit:cover;display:block}
.card .info{padding:10px}
.card .info h3{margin:0;font-size:16px}
.card .info p{margin:6px 0 0 0;font-size:13px;color:#ddd}

/* TIMELINE DOTS for mobile vertical timeline */
.vertical-line{position:relative;margin-left:20px;padding-left:18px;border-left:2px solid rgba(255,255,255,0.04)}
.vertical-dot{width:12px;height:12px;border-radius:50%;background:var(--mcu-red);box-shadow:0 0 10px var(--mcu-yellow) inset;margin-bottom:18px;}

/* MODAL */
.modal-backdrop{position:fixed;inset:0;background:linear-gradient(180deg, rgba(0,0,0,0.75), rgba(0,0,0,0.85));display:flex;align-items:center;justify-content:center;z-index:20;padding:20px}
.modal{background:#0b0b0b;border-radius:12px;max-width:980px;width:100%;display:grid;grid-template-columns:320px 1fr;gap:20px;padding:18px}
.modal .poster{width:100%;height:420px;object-fit:cover;border-radius:8px}
.modal h2{margin:0 0 6px 0}
.modal p{margin:8px 0;color:#ddd}
.close-btn{position:absolute;top:12px;right:12px;background:transparent;border:0;color:#fff;font-size:22px;cursor:pointer}

/* small screens adjustments */
@media(max-width:900px){
  .modal{grid-template-columns:1fr;align-items:start}
  .modal .poster{height:260px}
  .chip{width:36vw}
}
@media(max-width:640px){
  header.mcu-header.active{padding:18px 12px}
  .controls{flex-direction:column;gap:10px;padding:12px}
  .search{min-width:0;width:100%}
  .chip{width:70vw}
  body.show-intro-short #intro .title{transform:none}
}

/* subtle pulses & animations */
@keyframes pulse {
  0%{box-shadow:0 0 0 0 rgba(255,255,0,0.18)}
  70%{box-shadow:0 0 0 12px rgba(255,255,0,0.02)}
  100%{box-shadow:0 0 0 0 rgba(255,255,0,0)}
}
.pulse{animation:pulse 2s infinite}
main{position:relative;z-index:2;padding-bottom:80px}
